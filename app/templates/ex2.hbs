{{page-title "Ex2"}}
<h1> Service </h1>
<ul>
  {{#each @model.services as |service|}}
    <li {{action "toggleActive" service.name }}>{{service.name}}</li>
  {{/each}}
</ul>
<p > {{plural this.nbrActive}} </p>
<p>total : {{format-currency this.total '€'}} </p>
{{outlet}}
